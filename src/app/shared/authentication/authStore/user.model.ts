// Define an interface named 'User' to represent the structure of a user
export interface User {
    // Declare a property named 'userData' which is an object with 'email' and 'password' properties
    userData: {
        email: string,     // Property to store the user's email address
        password: string   // Property to store the user's password
    }
}
